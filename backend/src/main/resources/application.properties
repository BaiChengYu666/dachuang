# ======================
# 服务器配置
# ======================
server.port=8080

# ======================
# 数据库配置
# ======================
spring.datasource.url=jdbc:mysql://localhost:3306/health_db?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ======================
# SQL 初始化配置
# ======================
spring.sql.init.mode=always

# ======================
# JPA 配置
# ======================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# ======================
# 日志配置
# ======================
logging.level.org.springframework=INFO
logging.level.org.example.backend=DEBUG

# ======================
# JSON配置
# ======================
spring.jackson.time-zone=GMT+8
spring.jackson.date-format=yyyy-MM-dd HH:mm:ss

# ======================
# 文件上传配置
# ======================
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# ======================
# DeepSeek AI配置
# ======================
deepseek.api.key=sk-7b15a546ab71473facc7cb502e92ad10
deepseek.api.url=https://api.deepseek.com/v1/chat/completions
deepseek.model=deepseek-chat
